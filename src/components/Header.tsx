import React from 'react';

const Header: React.FC = () => {
    return (
        <header id="ui-header">
            <h1>Tactical Building Clearance</h1>
            <div id="status-bar">Intel needed...</div>
        </header>
    );
};

export default Header;
